"use clint";

import { ShoppingCart } from "lucide-react";

const CartButton = () => {
  return (
    <div>
      <ShoppingCart className="cursor-pointer" />
    </div>
  );
};

export default CartButton;
